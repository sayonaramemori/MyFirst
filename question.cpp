#include<vector>
#include<algorithm>

using namespace std;

class Solution {
public:
    double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) 
    {
        if()
        do_work(nums1,nums2);
    }
private:
    bool is_cate(vector<int>& nums1, vector<int>& nums2);
    int counts=0;
    int target_counts=0;  
    pair<int,int> num_spec;
};
bool Solution::is_cate(vector<int>& nums1, vector<int>& nums2)
{
    if(nums1[0])
}
double Solution::quick_find(vector<int>& nums1, vector<int>& nums2)
{
    
    if(nums2[0]>=nums1[nums1.size()-1])
    {

    }
    else
    {

    }
}
double Solution::double_find(vector<int>& nums1, vector<int>& nums2)
{
    int index2=index1=-1;
    //if even-num
    if(num_spec.first==num_spec.second)
    {
        int size1=nums1.size();
        int size2=nums2.size();
        for(int i=0;i<size1;++i)
        {
            if()
        }
    }
    else
    {

    }
}
void Solution::do_work(vector<int>& nums1, vector<int>& nums2)
{
    if(nums1.size()!=0&&nums2.size()!=0)
    {
        
    }
    else
    {
        if(nums1.size()!=0)
        {

        }
        if(nums2.size()!=0)
        {

        }
    }
}


int main()
{
    system();
    return 0;
}